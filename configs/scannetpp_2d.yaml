# Minimal ScanNet++ preprocessing config. Command-line flags override these values.
data_root: /home/bod/184Nas/open_source/scannet_pp/data
output_root: data/processed/scannetpp_2d

# Choose a subset while developing. You can also use scene_list instead.
scene_ids: []
scene_list:
limit_scenes:

image_subdir: dslr/resized_images
mask_subdir: dslr/resized_anon_masks
colmap_subdir: dslr/colmap
scan_subdir: scans

split: all
frame_step: 1
max_frames:
frame_list:

near: 0.001
semantic_ignore_label: 65535
save_visualizations: true
save_raster: true
save_pointmaps: true
apply_anon_mask: true
skip_existing: true
dry_run: false
