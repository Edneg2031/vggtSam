# Debug config for inspecting SAM3/VGGT outputs before fixing a fusion layer.
dataset:
  manifest: data/processed/scannetpp_2d/manifest.json
  scene_id: 0a5c013435
  num_frames: 4

sam3:
  repo: externals/sam3
  checkpoint: /home/bod/86Nas/95_data_bak/FoundationModels/sam3/sam3.pt
  prompt: chair
  gpus_to_use: [0]

vggt:
  repo: externals/vggt
  checkpoint: /home/wlh50060092/FoundationCode/vggt/checkpoints.pth
  patch_multiple: 14

runtime:
  device: cuda
  output_json: outputs/debug/backbone_outputs.json
