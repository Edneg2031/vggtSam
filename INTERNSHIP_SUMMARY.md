# 实习总结：SAM 物体锚点修正 HorizonStream 位姿漂移

2026-09-18 · 实习工作交接

**摘要**：在冻结的 HorizonStream 上，用 SAM3.1 的 persistent object tracks 作为跨帧物体锚点，检测并修正流式累积位姿的漂移，并把修正反馈回后续帧。两个帧窗的 direct ATE 分别改善 **+14.25%**（100 帧）与 **+15.4~15.6%**（150 帧），均通过全部七条事前判据；同一修正传导至点云产物，object accuracy −29%、ghost −44%、F5cm +12%。结论适用范围有限（单一场景、开发窗口、无 held-out 证据）。

方法和实验细节见 [`streaming_couping/docs/report.md`](streaming_couping/docs/report.md)；运行方式、代码结构与已知风险见 [`HANDOVER.md`](HANDOVER.md)。

---

## 1 任务与背景

HorizonStream 是流式几何基础模型，逐帧输出 metric depth、depth confidence、intrinsics 与相机位姿。其位姿由运行时累积器（`online_motion_averaging`）在线因果产生，**误差会累积且不自纠正** —— 模型本身没有任何跨帧的景物级约束。

本课题验证的问题是：**SAM3.1 的 persistent object tracks 能否作为跨帧稳定的物体锚点，检测并修正该累计位姿漂移，且把修正反馈进后续帧。**

约束条件（全程未突破）：不训练任何模型；不修改 HorizonStream 的 backbone / KV / GLA cache；不引入 DINO 或任何 learned score；GT 仅在所有反馈决策冻结之后加载，仅用于评测。

---

## 2 方法与关键设计

### 2.1 技术路线

对每个 (帧, 实例)，以 mask ∩ 有效深度取最多 256 个**相机系**点，与该实例的参考云做最近点对齐，解一个 6DoF 增量；各物体的 `log(ΔT)` 经加权 Huber IRLS 取**加权中位数**得到每帧修正；修正通过门控后，作为**绝对目标**写入累积器。

整体为**两遍式**：提案基于 raw 几何一次算完，修正之后仅在 CPU 上因果重放累积器。因此**修正在传播上闭环、在估计上开环**。

### 2.2 三个关键设计决策

**一、共识权重只用 `S_sem`，不叠加 `S_geo`。** 两个为"物体质量"设计的可靠性分数经实测均为**反预测**（`S_sem` 类内 ρ = +0.634，`S_geo` = +0.342）。叠加 `S_geo` 使增益由 +14.25% 减半至 +7.83%。真正有效的是两个**一致性**判据（`consensus_inlier`、帧级 gate），而它们必须已有共识才能计算。

**二、注入绝对目标而非增量。** anchor（前 5 帧）钉在 raw 世界系上，因此重复注入是**覆盖**而非叠加，不会累积成过修正。

**三、判据事前声明，且只看位姿指标。** 七条判据在实验前确定，从不依据 ICP loss —— 已有实例显示 loss 下降 58% 而 GT 指标持平。

---

## 3 成果

### 3.1 位姿主结果

| 帧窗 | raw direct ATE | proposals | accepted | d_ATE | d_sim3 | 判定 |
|---|---:|---:|---:|---:|---:|---|
| 100 帧 | 0.1166 m | 229 | 33 | **+14.25%** | **+5.35%** | **GO（7/7）** |
| 150 帧 | 0.1095 m | 335 | 33 | **+15.4~15.6%** | 正 | **GO（7/7）** |

150 帧的区间来自两次独立的 stage-1 运行（+15.60% 与 +15.36%），其差值即**跨运行误差**。`d_sim3 > 0` 说明增益**不是**吸收一个全局相似变换的结果，而是相对几何本身改善。

### 3.2 向点云产物的传导

位姿指标改善不等同于产物改善，因此单独测量：将每个观测的**同一批**相机系点，分别以 raw 轨迹与修正轨迹置入世界（点集与位姿来源分离，故身份错误、mask 质量与采样因素全部抵消）。池化 `[all]`，100 帧：

| 指标 | raw | 修正后 | Δ |
|---|---:|---:|---:|
| object_accuracy_m ↓ | 0.0167 | **0.0119** | **−29%** |
| object_completeness_m ↓ | 0.0947 | **0.0884** | −6.7% |
| fscore_5cm ↑ | 0.4895 | **0.5487** | **+12.1%** |
| voxel_iou_5cm ↑ | 0.1142 | 0.1144 | 持平 |
| ghost_point_ratio ↓ | 0.0926 | **0.0518** | **−44%** |

**通过轨迹判据的三个变体，同时是点云指标最优的三个** —— 两项独立测量给出一致排序。

### 3.3 机制验证与稳健性

**环路本身可用。** 在引入 SAM 之前先行验证：把**已知正确**的修正（取自 GT）单次写回累积器，此后 t+1..t+10 的 translation 位移 **10/10 全部改善**。该结果把"环路能否接受并传播修正"与"能否估出该修正"分离，前者成立。

**不依赖单一物体。** v3 = v1 移除 `dustbin`（GT 侧评分最优类别）后，100 帧 +14.25%、150 帧 +15.35%，与 v1 相同且仍为 GO。

**两种噪声底必须区分。** 共享同一次几何缓存时为 **1.8e-05**；跨独立 stage-1 运行时为 **2.4e-03**，约前者的 **130 倍**。判断差异是否显著时须使用与比较方式对应的那一个。

### 3.4 工程产出

运行入口收敛为**唯一一条链路**（3 个命令文件，缺一不可）；文档由 16 份合并为 2 份；主链路每轮运行即产出全部结论性内容 —— 判定与判据表、逐代对照表、两张对比图、候选账本摘要，以及完整日志。

---

## 4 结论的适用范围

| 项 | 状态 |
|---|---|
| 场景数 | **1**（`00a231a370`） |
| 帧窗 | 2 个，为**同一场景的复核**，非第二个场景 |
| 协议角色 | 开发窗口；判据与阈值即在该窗口上确定，**无 held-out 证据** |
| prompt 集 | **方法的一部分，非自由参数** |
| 点云绝对值 | **不可引用**（预测云稀疏约 100 倍），仅 raw 与修正的**对比**有效 |
| 改善分布 | **不均匀** —— `bed` 五项全部改善，`chair` / `wardrobe` 为 F5cm 上升而 ghost / IoU 下降 |

上述每一条都已写入文档，建议在引用本结果时一并引用本节。

---

## 5 交接计划

### 5.1 交接物

| 内容 | 位置 |
|---|---|
| 交接文档（运行方式、环境、代码结构、已知风险、后续建议） | `HANDOVER.md` |
| 技术报告（方法 §1–§9、实验 §10–§20） | `streaming_couping/docs/report.md` |
| 主执行链路（唯一入口及其两个下游） | `streaming_couping/commands_*.txt` |
| 实现 | `streaming_couping/src/`、`streaming_couping/scripts/` |
| 单元测试（231 通过 / 1 失败 / 3 跳过） | `streaming_couping/tests/` |
| 环境探查脚本 | `streaming_couping/scripts/report_environment.py` |

### 5.2 运行方式与环境

运行前**必须先激活 `3am` 环境** —— 入口脚本以 `$(command -v python)` 解析 Stage 2/3 的解释器：

```bash
conda activate 3am
zsh streaming_couping/commands_run_scannet_object_pose_feedback_branches.txt
```

Stage 1 所用的 `horizonstream` 环境（PyTorch 2.8.0+cu128）由脚本内部按固定路径调用，**不需要激活**；两套环境的 torch / CUDA 版本不同，**不可交叉调用**。完整环境清单见 `HANDOVER.md` §3.1。

### 5.3 已排除的技术方向

以下方向均已实测，**不建议重复投入**；检验方式与完整数据见 `streaming_couping/docs/report.md` §15–§16。

| 方向 | 结论 |
|---|---|
| 改进分割质量 | GT mask 替换 SAM 后提案误差**反而更差**（0.1010 对 0.0892），瓶颈不在 mask |
| 更新参考集 | anchor 年龄由 37.5 降至 8，提案误差**无变化**（0.0893→0.0891）；原相关为混淆所致 |
| 旋转与平移解耦 | 可全部通过判据，但跨配置摆动于 **+3% 与 +19%** 之间，属不稳定配置 |
| 仅修正平移 | 显著更差，sim3 转负；旋转与平移在联合求解中**耦合** |
| 扩充 prompt | 单次增词即挤占全局名额并触发判重，结果由 +15.36% 跌至 **−11.87%** |
| 闭环 / 流式 | 提案残差降 **18.0%**，轨迹反而差 **3.6%**，全部变体 NO_GO |
| 更换优化目标 | 目标函数对**共模误差免疫**（`P`、`Q` 同源），无法感知其贡献的绝对位姿误差 |

### 5.4 需团队决定的问题

**1. `outputs/` 目录的去留。** 两次独立 stage-1 运行的 d_ATE 相差约 **0.24 个百分点**，大于多个分支之间的差异；主链路会让新一代几何缓存自上一代复制，以保持两代可比。**删除该目录即失去这一条件，且无法事后恢复。** 需要决定保留多久、由谁维护，以及是否需要建立 run 目录到报告数字的对应表。

**2. `src/` 中旧线模块是否清理。** 仓库中仍留有上一代（StreamVGGT / 语义地图线）的模块。注意：**静态导入分析在本仓库不可靠**（精简过程中出现三次误判），判断归属应依据模块 docstring。

**3. 既有失败测试如何处理。** `test_instance_point_consistency.py::test_instance_consistency_is_causal_and_rejects_far_points`（`assert 4 == 3`）在本轮精简之前即存在，与本课题主链路无关。修 / 标记 xfail / 保持现状，三者需选一。

**4. 一处配置遗留。** `configs/recovery_dynamic_instance.yaml` 中仍保留 StreamVGGT 条目（`device: cuda:0`），当前主链路不读取，可一并移除。

### 5.5 后续工作建议

1. **补充第二个场景。** 当前结论的场景数为 1 且位于开发窗口。这是把结论由"在该窗口上成立"提升为"该方法成立"的**唯一途径**，其余工作均为次要。
2. **查清 `anchor_rho` 的消失。** 该量在 100 帧上是最强预测因子（类内 ρ = 0.79，`fresh` 分支即为此设计），在 150 帧上却降至 0.09。可能是测量问题，也可能指向尚未发现的机制。
3. **分离单次增词实验的归因。** 需构造"新词进入而原词不丢失"的条件（提高 `--max-objects` 或调整判重排序），以确定劣化源于丢失原词还是引入新词。
4. **建立物体预筛选判据。** 现有判据分三类且生效时机不同（见 `report.md` §5）；若要继续提升，可改的是**跟踪层规则**（16 条全局名额、按出生帧判重），而非再加一个物体质量打分器。
