# 当前阶段：StreamVGGT 原始点图与相机位姿诊断。
#
# 不运行 SAM3，不使用实例 mask，不修改 pointmap 或 pose。主指标使用 reference
# 帧全场景 paired pointmap 拟合的一次固定 Sim(3)，并同时输出：
#   reference_point_sim3       主评估；检查 pose head 与 point head/GT 的一致性
#   reference_pose_point_scale 首帧 pose 对齐；检查后续相对漂移
#   trajectory_sim3            全轨迹拟合；仅作乐观 gauge 参照
mkdir -p outputs
set -o pipefail

PYTHONUNBUFFERED=1 PYTHONPATH=src:. python -m streaming_couping.scripts.run_pose_pointmap_diagnostics \
  --config streaming_couping/configs/recovery_050_025.yaml \
  --manifest data/processed/scannetpp_pinhole_2d/manifest.json \
  --scene-id 00a231a370 \
  --frame-indices 90 105 119 130 140 210 240 \
  --reference-sequence-index 0 \
  --geometry-device cuda:1 \
  --output-dir outputs/streaming_couping_pose_pointmap_raw_00a231a370 \
  2>&1 | tee outputs/streaming_couping_pose_pointmap_raw_00a231a370.log

# 运行结束后回传以下小文件，不需要回传 PLY：
#   outputs/streaming_couping_pose_pointmap_raw_00a231a370/pose_summary.csv
#   outputs/streaming_couping_pose_pointmap_raw_00a231a370/pose_frame_metrics.csv
#   outputs/streaming_couping_pose_pointmap_raw_00a231a370/pose_rpe.csv
#   outputs/streaming_couping_pose_pointmap_raw_00a231a370/pose_pair_summary.csv
#   outputs/streaming_couping_pose_pointmap_raw_00a231a370/pointmap_summary.csv
#   outputs/streaming_couping_pose_pointmap_raw_00a231a370/pointmap_frame_metrics.csv
#   outputs/streaming_couping_pose_pointmap_raw_00a231a370/intrinsics_summary.csv
#   outputs/streaming_couping_pose_pointmap_raw_00a231a370/metadata.json
#   outputs/streaming_couping_pose_pointmap_raw_00a231a370.log
