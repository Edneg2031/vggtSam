#!/usr/bin/env zsh
set -euo pipefail

# Per-instance variant of the object-only loss experiment.  HorizonStream
# camera poses and background geometry remain raw; each accepted SAM instance
# receives its own 6DoF object-point correction.
ROOT="${VGGT_SAM_ROOT:-/home/wlh50060092/vggtSam}"
STORAGE_ROOT="${VGGT_SAM_STORAGE_ROOT:-/data184/open_source/vggtSam}"
OUTPUT_DIR="${OBJECT_POSE_LOSS_OBJECT_PER_INSTANCE_OUTPUT_DIR:-$STORAGE_ROOT/outputs/semantic_map_100frames_horizonstream_object_pose_loss_object_per_instance_v1}"

export OBJECT_POSE_LOSS_OBJECT_ONLY_OUTPUT_DIR="$OUTPUT_DIR"
export OBJECT_POSE_LOSS_OBJECT_ONLY_INDEPENDENT_INSTANCES=1
export OBJECT_POSE_ALIGNED_BRANCH=object_pose_object_only_per_instance

exec zsh "$ROOT/streaming_couping/commands_run_scannet_object_pose_loss_object_only_100f.txt"
