# V9.7: full-grid descriptor causality after the V9.6 coordinate gate.
v96_config: streaming_couping/configs/v96_dense_grid_upper_bound.yaml
data_config: streaming_couping/configs/v92_support_data.yaml
output_dir: outputs/streaming_couping_v97_dense_descriptor_causality
dense_cache_path: outputs/streaming_couping_v97_dense_descriptor_causality/cache/00a231a370_90_525_step15_dense.pt
clip_name: 00a231a370_90_525_step15_37_68_54

grid_size: [72, 72]
cache_device: cuda:0
cache_batch_size: 2

# Training support is causal and does not read pose error.
training_query_count: 32
training_history_offsets: [1, 2]

matcher:
  canonical_dim: 256
  projection_dim: 64
  offset_hidden_dim: 128
  temperature: 0.07
  offset_weight: 1.0
  pck_threshold_pixels: 1.0

training:
  device: cuda:0
  seed: 97
  steps: 600
  batch_pairs: 4
  learning_rate: 0.0003
  weight_decay: 0.0001
  log_every: 50

