# V9.1: no training, no pose adapter, no hyperparameter sweep.
stage_a_config: streaming_couping/configs/v90_local_token_matcher.yaml
stage_a_output_dir: outputs/streaming_couping_v90_epipolar_token_stage_a
output_dir: outputs/streaming_couping_v91_token_evidence_audit

# Raw descriptor diagnostic only. Trained Q/K uses the locked V9 Stage-A config.
raw_temperature: 0.07
raw_dustbin_logit: 0.0

# Fixed reporting thresholds. The A0-Q solver support remains the Stage-A 12px radius.
coverage_thresholds_pixels: [8.0, 12.0, 16.0]
