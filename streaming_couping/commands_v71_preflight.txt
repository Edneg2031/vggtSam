#!/usr/bin/env zsh
set -euo pipefail

ROOT="${VGGT_SAM_ROOT:-/home/wlh50060092/vggtSam}"
PYTHON_BIN="${STREAMING_COUPING_PYTHON:-/home/huawei/miniconda3/envs/3am/bin/python}"

cd "$ROOT"
[[ -x "$PYTHON_BIN" ]] || { echo "missing Python: $PYTHON_BIN" >&2; exit 1; }

echo "preflight 1/3: frozen-cache shape/finite/metadata audit"
PYTHONPATH=. "$PYTHON_BIN" \
  -m streaming_couping.scripts.audit_v7_cache \
  --config streaming_couping/configs/v7_fusion_data.yaml \
  --output-dir outputs/streaming_couping_v71_preflight/cache_audit \
  --strict

echo "preflight 2/3: causal and reporting unit tests"
PYTHONPATH=. "$PYTHON_BIN" -m pytest -q \
  streaming_couping/tests/test_v7_fusion.py \
  streaming_couping/tests/test_v71_causal_fusion.py \
  streaming_couping/tests/test_v71_reporting.py

echo "preflight 3/3: all V7.1 architectures forward/backward for two steps"
zsh streaming_couping/commands_v71_smoke.txt
echo "V7.1 preflight passed"
